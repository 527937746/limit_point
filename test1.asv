clear

r = 0:0.01:10;
theta = (0:0.01:1).*2*pi;
X = zeros(length(r),length(theta));
Y = zeros(length(r),length(theta));
Z = zeros(length(r),length(theta));

for i = 1:length(r)
    for j = 1:length(theta)
        [x(i),y(i)] = p_to_c(r(i),theta(j));
        
    end
end